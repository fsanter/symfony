{% extends 'base.html.twig' %}

{% block body %}
    {{ article.title }}
    <p>Tags associés :</p>
    {# quand on fait article.tags, une requête SQL
        est déclenchée pour récupérer les tags
        associés à l'article
    #}

    {% for tag in article.tags %}
        {{ tag.label }}<br>
    {% endfor %}

    {# pourquoi peut-on accéder à la propriété
        tags directement alors qu'elle est privée ?
        https://openclassrooms.com/fr/courses/3619856-developpez-votre-site-web-avec-le-framework-symfony/3621582-le-moteur-de-templates-twig
        Dans l'ordre, en fait il teste :
        - article.tags ? non privé
        - article.tags() ? n'existe pas
        - article.getTags() ? existe et public
        - twig aurait une dernière tentative : article.isTags()
    #}
{% endblock %}